Run `scratch_cache="$(mktemp -d)" npm_config_cache="$scratch_cache" npx --yes github:grayscale-development/engram init` in the directory you want to set up.
Copy to your agent: `Read .engram/skills/onboarding/SKILL.md and follow it. Offer me Orientation or Quick setup; if I choose Quick setup, skip the orientation.`
